{
  "$railway": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "echo 'Skipping frontend build for Railway backend service'",
    "watchPatterns": [
      "railway.json",
      "package.json",
      "package-lock.json",
      "dessert-ai-system/**"
    ]
  },
  "deploy": {
    "startCommand": "if [ -f dessert-ai-system/server/server.js ]; then node dessert-ai-system/server/server.js; elif [ -f server/server.js ]; then node server/server.js; elif [ -f server.js ]; then node server.js; else npm start; fi",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
